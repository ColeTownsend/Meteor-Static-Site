module.exports = function(config, env) {
  // config.removeLoader('css');
  // config.loader('css', function(cfg) {
  //   cfg.test = /\.scss$/;
  //   cfg.loaders = ["style", "css", "sass"]
  //   return cfg
  // })
  return config;
};

// css modules?
// sass
